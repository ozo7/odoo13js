odoo.define('awesome_tshirt.Executioner', function (require) {
    "use strict";

    const AbstractAction = require('web.AbstractAction');

    const ClientAction = AbstractAction.extend({
        // RE -- is this working and actually triggered by the menu item?
        // RE -- is the init loaded now with the client action triggered ?
        const Testing = require('awesome_tshirt.Simple');

    });


});